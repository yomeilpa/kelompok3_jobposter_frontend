import { JobRecuitmentModel } from './job-recuitment-model';

describe('JobRecuitmentModel', () => {
  it('should create an instance', () => {
    expect(new JobRecuitmentModel()).toBeTruthy();
  });
});
