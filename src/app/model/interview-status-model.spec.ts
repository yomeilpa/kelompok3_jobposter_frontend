import { InterviewStatusModel } from './interview-status-model';

describe('InterviewStatusModel', () => {
  it('should create an instance', () => {
    expect(new InterviewStatusModel()).toBeTruthy();
  });
});
