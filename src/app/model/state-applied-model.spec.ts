import { StateAppliedModel } from './state-applied-model';

describe('StateAppliedModel', () => {
  it('should create an instance', () => {
    expect(new StateAppliedModel()).toBeTruthy();
  });
});
